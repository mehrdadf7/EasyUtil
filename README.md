# EasyUtil
Useful Library

## options
    .Custom AlertDialog
    .Check PhoneNumber
    .Pagination(Load More For RecyclerView)
    .EasyLog by TAG "log"
    .EasyToast
    .EasyVisibility
    .EasyAnimation by YoYo
